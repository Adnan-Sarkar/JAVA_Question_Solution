package summer_2020;

/*
It will give us Error. because we can't access anything(attributes/methods) in child class through parent class
reference variable. speak() method isn't override, So we can't access it through bird (parent type) reference
variable.
*/
